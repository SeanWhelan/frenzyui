import { Button } from './Button'
import { Input } from './Input'

describe('Button', () => {
  it('is truthy', () => {
    expect(Button).toBeTruthy()
  })
})

describe('Input', () => {
  it('is truthy', () => {
    expect(Input).toBeTruthy()
  })
})
